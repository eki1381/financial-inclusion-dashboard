'use client'

import { DashboardData } from '@/lib/types'

interface EconomicGrowthSectionProps {
  data: DashboardData
}

export function EconomicGrowthSection({ data }: EconomicGrowthSectionProps) {
  return (
    <section className="py-20 bg-gray-50">
      <div className="container mx-auto px-6">
        {/* Section dikosongkan sesuai permintaan */}
      </div>
    </section>
  )
}
